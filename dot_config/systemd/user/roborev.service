[Unit]
Description=Roborev review daemon
After=default.target

[Service]
Type=exec
Environment=PATH=%h/.local/bin:%h/.bun/bin:%h/.cargo/bin:%h/go/bin:/usr/local/bin:/usr/bin:/bin
ExecStart=%h/.local/bin/roborev daemon run
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
