[Unit]
Description=Kata issue tracker daemon
After=default.target

[Service]
Type=exec
Environment=PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin
ExecStart=/usr/bin/env kata daemon start
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
